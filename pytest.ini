[pytest]
; Ensures the repository root is always on sys.path, so `backend` is
; importable as a package regardless of how pytest is invoked (plain
; `pytest`, `python -m pytest`, or from an IDE test runner).
pythonpath = .
addopts = -ra
